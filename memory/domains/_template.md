---
title:
도메인명: null
category: domain
service:
  잡코리아 | 알바몬 | 통합플랫폼 | 공통: null
tags:
- domain
related_projects: []
created_at: YYYY-MM-DD
last_reviewed: YYYY-MM-DD
permalink: memory/domains/template
---

## 개요

> 이 문서가 다루는 대상과 목적을 2~3줄로 설명.  
> 예) "jk-bff-display-api의 스마트핏 추천 API 분석"  
> 예) "display-jarvis 전체 모듈 간 의존 관계 및 데이터 흐름 정리"  
> 예) "공고 상세 조회 기능의 캐싱 전략 분석"

## 아키텍처 / 구조 (서비스·모듈 단위일 때)

> 모듈 간 관계, 전체 흐름, 레이어 구조 등 큰 그림을 기술할 때 사용

```
{전체 구조 다이어그램 또는 흐름도}
예)
Client
  └─ jk-bff-display-api
       ├─ SmartFitService  →  외부추천API
       └─ DisplayService   →  공고DB
```

### 모듈 / 컴포넌트 역할 정리

| 모듈 / 컴포넌트 | 역할 | 주요 의존 대상 |
|---------------|------|--------------|
|               |      |              |

---

## API 명세 (API 단위일 때)

### `{HTTP_METHOD} {/api/path}`

**설명**:

**요청 파라미터**:
- `{param1}`: {type} — {설명} (필수/선택)
- `{param2}`: {type} — {설명} (필수/선택)

**요청 바디** (있는 경우):
```json
{
  "field1": "type",
  "field2": "type"
}
```

**응답**:
```json
{
  "field1": "type",
  "field2": "type"
}
```

**에러 케이스**:
| 코드 | 상황 |
|------|------|
|      |      |

---

## 소스 흐름 분석 (기능·API 단위일 때)

```
{진입점}.{method}()
  └─ {Service}.{method}()
       ├─ {Repository}.{query}()        # DB 조회
       ├─ {ExternalClient}.{call}()     # 외부 API 호출
       └─ {Mapper}.{convert}()          # 변환 로직
```

### 계층별 상세

| 계층 | 파일 경로 | 핵심 내용 |
|------|---------|---------|
| Controller | `{module}/.../{Name}Controller` | |
| Service | `{module}/.../{Name}Service` | |
| Repository | `{module}/.../{Name}Repository` | |

### 외부 서비스 호출

| 서비스명 | 엔드포인트 | 용도 | 호출 방식 |
|---------|-----------|------|---------|
|         |           |      |         |

---

## 도메인 모델 / 데이터 구조

```java
// 주요 DTO, Domain 객체, 또는 테이블 구조
class {Name} {
    // 필드 목록
}
```

---

## 비즈니스 규칙

> 코드에서 파악한 주요 비즈니스 제약 조건, 예외 케이스 등

-
-

---

## 발견된 문제점

- [ ] {문제}: {설명}
- [ ] {문제}: {설명}

---

## 작업 이력

### YYYY-MM-DD — {작업 요약}
- **작업 유형**: 분석 / 리팩토링 / 구현 / 디버깅
- **변경 내용**:
- **변경 이유**:
- **영향 범위**:

---

## 관련 도메인 파일

- `memory/domains/{관련파일}.md`


## 상태 전이

### {테이블명}.{컬럼명}

```
(상태 전이 다이어그램을 텍스트로 표현)
```

- 상태1: 설명
- 상태2: 설명

### 상태 전이 조건

| 전이 | 조건 | 처리 주체 |
|------|------|-----------|
| A → B | (어떤 조건에서) | (Java 코드 or 프로시저 or 배치) |

## DB 스키마

### 핵심 테이블

(전체 컬럼이 아니라 비즈니스적으로 중요한 컬럼 위주로)

예시 — 핵심 테이블 샘플 (비즈니스 관점에서 주요 컬럼만 기재):

**orders** — 주문 정보
- order_id (PK), member_id (FK), delivery_id (FK), order_date, status

**order_item** — 주문별 아이템
- order_item_id (PK), order_id (FK), item_id (FK), order_price, count

**item** — 상품 정보
- item_id (PK), name, price, stock_quantity

설명: 위 예시는 도메인 문서 작성 시 빠르게 핵심 테이블과 중요한 컬럼을 파악할 수 있도록 돕습니다. 실제 문서에는 ERD 이미지(예: ERD.png) 또는 마이그레이션 파일 경로를 링크해 두는 것을 권장합니다.

### 테이블 관계

```
(테이블 간 관계를 텍스트로 표현)
```

## 프로시저 / 함수

### {프로시저명}
- 위치: {DB명}
- 역할: (무엇을 하는지)
- 호출: (어디서 호출하는지)
- **주의: (비즈니스 로직이 프로시저에 있는 경우 반드시 명시)**

## 로직 소재지

| 기능 | 처리 위치 | 비고 |
|------|-----------|------|
| (기능1) | Java (클래스명) | |
| (기능2) | **프로시저** (프로시저명) | (특이사항) |
| (기능3) | 프론트 (파일명) | |
| (기능4) | **배치** (배치명) | |

## 구현체별 차이

(레거시/신규 구현이 공존하는 경우 이 섹션으로 구분.
 필요 없으면 이 섹션은 삭제.)

### 잡코리아 PC (레거시)
- 소스: `~/IdeaProjects/{프로젝트명}/...`
- (구현 방식, 차이점)

### 잡코리아 Mobile (레거시)
- 소스: `~/IdeaProjects/{프로젝트명}/...`
- (구현 방식, 차이점)

### 신규 MSA
- 소스: `~/IdeaProjects/{프로젝트명}/...`
- (구현 방식, 차이점)

## 관여하는 서비스와 소스 위치

### {역할 설명}
- [[{프로젝트 카드 wikilink}]]
- `~/IdeaProjects/{프로젝트명}/src/.../{ }.java`

## 관련 노트

- [[{프로젝트 카드}]]
- [[{의사결정 노트}]]
- [[{아이디어 노트}]]